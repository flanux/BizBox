@model Bizbox.ViewModels.AdminProductFormViewModel
@{
    ViewData["Title"] = "Add Product";
}

<div class="admin-form-shell">
    <div class="page-header">
        <span class="eyebrow">Catalog / New</span>
        <h1>Add Product</h1>
        <p>Create a platform product. Images are uploaded directly and stored under <code>wwwroot/uploads/products</code>.</p>
    </div>

    <partial name="_ProductForm" model="Model" />
</div>
